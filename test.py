import numpy as np
import random
def Solve(N, M):
    n = np.zeros((N,N))
    # 矩阵赋值
    for i in range(0, N):
        for j in range(0, N):
            n[i][j] = random.randint(0,N)

    for m in range()
